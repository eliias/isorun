{
  "name": "rails-react-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@apollo/client": "^3.7.9",
    "@headlessui/react": "^1.7.12",
    "@heroicons/react": "^2.0.12",
    "@hotwired/stimulus": "^3.1.0",
    "@hotwired/turbo-rails": "^7.2.4",
    "@isorun/rails": "*",
    "esbuild": "^0.15.12",
    "graphql": "^16.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/client/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets",
    "build-server": "esbuild app/javascript/server/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=assets --format=esm"
  }
}
